import { Body, Controller, Param, Post } from '@nestjs/common'; import { LogisticsService } from './logistics.service'; import { CurrentAuth } from '../../common/request-context'; import type { AuthContext } from '../../common/authorization.service'; @Controller('deliveries') export class LogisticsController{constructor(private s:LogisticsService){} @Post(':id/receive')receive(@CurrentAuth()a:AuthContext,@Param('id')id:string,@Body()b:any){return this.s.receive(a.organizationId,id,b.items??[])}}
