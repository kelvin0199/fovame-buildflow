import { Body, Controller, Post } from '@nestjs/common'; import { InventoryService } from './inventory.service'; import { CurrentAuth } from '../../common/request-context'; import type { AuthContext } from '../../common/authorization.service'; import { randomUUID } from 'crypto'; @Controller('inventory') export class InventoryController{constructor(private s:InventoryService){} @Post('issues')issue(@CurrentAuth()a:AuthContext,@Body()b:any){return this.s.issue(a.organizationId,{...b,actor_id:a.userId,request_id:b.request_id??randomUUID()})}}
