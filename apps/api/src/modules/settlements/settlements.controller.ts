import { Controller, Param, Post } from '@nestjs/common'; import { SettlementsService } from './settlements.service'; import { CurrentAuth } from '../../common/request-context'; import type { AuthContext } from '../../common/authorization.service'; @Controller('settlements') export class SettlementsController{constructor(private s:SettlementsService){} @Post(':id/approve')approve(@CurrentAuth()a:AuthContext,@Param('id')id:string){return this.s.approve(a.organizationId,id,a.userId)}}
