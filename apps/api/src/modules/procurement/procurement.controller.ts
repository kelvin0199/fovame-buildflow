import { Body, Controller, Get, Param, Post } from '@nestjs/common'; import { ProcurementService } from './procurement.service'; import { CurrentAuth } from '../../common/request-context'; import type { AuthContext } from '../../common/authorization.service';
@Controller('rfqs') export class ProcurementController {constructor(private s:ProcurementService){} @Post() create(@CurrentAuth()a:AuthContext,@Body()b:any){return this.s.createRfq(a.organizationId,a.userId,b)} @Post(':id/publish') publish(@CurrentAuth()a:AuthContext,@Param('id')id:string){return this.s.publish(a.organizationId,id)} @Get(':id/comparison') compare(@CurrentAuth()a:AuthContext,@Param('id')id:string){return this.s.comparison(a.organizationId,id)}}
