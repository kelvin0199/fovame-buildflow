import { Body, Controller, Post } from '@nestjs/common'; import { AiService } from './ai.service'; import { CurrentAuth } from '../../common/request-context'; import type { AuthContext } from '../../common/authorization.service'; @Controller('ai') export class AiController{constructor(private s:AiService){} @Post('assistant/query')query(@CurrentAuth()a:AuthContext,@Body()b:any){return this.s.assistant(a.organizationId,a.userId,b.prompt)} @Post('actions/propose')propose(@CurrentAuth()a:AuthContext,@Body()b:any){return this.s.propose(a.organizationId,a.userId,b.action_type,b.payload)}}
