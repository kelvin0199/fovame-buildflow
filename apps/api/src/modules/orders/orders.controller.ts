import { Controller, Get, Param } from '@nestjs/common'; import { OrdersService } from './orders.service'; import { CurrentAuth } from '../../common/request-context'; import type { AuthContext } from '../../common/authorization.service'; @Controller('purchase-orders') export class OrdersController{constructor(private s:OrdersService){} @Get()list(@CurrentAuth()a:AuthContext){return this.s.list(a.organizationId)} @Get(':id')get(@CurrentAuth()a:AuthContext,@Param('id')id:string){return this.s.get(a.organizationId,id)}}
