import { Injectable } from '@nestjs/common'; import { PrismaService } from '../../common/prisma.service'; import { statusProjection } from '../../common/status-projection';
@Injectable() export class OrdersService {constructor(private p:PrismaService){} async get(org:string,id:string){const po=await this.p.purchaseOrder.findFirstOrThrow({where:{id,organizationId:org}});const actions=po.status==='AWAITING_FUNDING'?['FUND_ORDER']:po.status==='FUNDED'?['CREATE_SHIPMENT']:[];return statusProjection(po,actions,actions[0]??null)} list(org:string){return this.p.purchaseOrder.findMany({where:{organizationId:org},orderBy:{updatedAt:'desc'}})}}
